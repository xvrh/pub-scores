name: pub_scores
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.3 <3.0.0'

dependencies:
  json_annotation:
  jsontool:

dev_dependencies:
  build_runner:
  collection:
  json_serializable:
  github:
  lints:
  pool:
  pub_api_client:
    git:
      url: https://github.com/xvrh/pub_api_client.git
      ref: f9e4e77d1f0a9a4b0eba1f9054bad7a4dc92f439
  retry:
  test:
